import { Contracts } from './Contracts';
import { BaseValue, SendOptions, TxResult, address, Price } from '../lib/types';
export declare class Admin {
    private contracts;
    private perpetual;
    constructor(contracts: Contracts);
    setGlobalOperator(operator: address, approved: boolean, options?: SendOptions): Promise<TxResult>;
    setOracle(oracle: address, options?: SendOptions): Promise<TxResult>;
    setFunder(funder: address, options?: SendOptions): Promise<TxResult>;
    setMinCollateral(minCollateral: BaseValue, options?: SendOptions): Promise<TxResult>;
    enableFinalSettlement(priceLowerBound: Price, priceUpperBound: Price, options?: SendOptions): Promise<TxResult>;
}
