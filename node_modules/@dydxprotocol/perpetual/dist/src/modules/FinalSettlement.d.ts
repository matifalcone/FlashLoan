import { Contracts } from './Contracts';
import { SendOptions, TxResult } from '../lib/types';
export declare class FinalSettlement {
    private contracts;
    private perpetual;
    constructor(contracts: Contracts);
    withdrawFinalSettlement(options?: SendOptions): Promise<TxResult>;
}
