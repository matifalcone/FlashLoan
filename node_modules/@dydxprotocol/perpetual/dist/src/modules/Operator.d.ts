import { Contracts } from './Contracts';
import { address, SendOptions, TxResult } from '../lib/types';
export declare class Operator {
    private contracts;
    private perpetual;
    constructor(contracts: Contracts);
    setLocalOperator(operator: address, approved: boolean, options?: SendOptions): Promise<TxResult>;
}
