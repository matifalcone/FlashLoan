import { Contracts } from '../modules/Contracts';
import { address, BaseValue, Price, CallOptions, SendOptions } from '../lib/types';
export declare class PriceOracle {
    private contracts;
    private oracles;
    constructor(contracts: Contracts);
    get address(): string;
    getPrice(options?: CallOptions): Promise<Price>;
    getRoute(sender: address, options?: CallOptions): Promise<address>;
    getOracleAdjustment(oracleAddress: address, options?: CallOptions): Promise<BaseValue>;
    setRoute(sender: address, oracle: address, options?: SendOptions): Promise<void>;
    setAdjustment(oracle: address, adjustment: BaseValue, options?: SendOptions): Promise<void>;
    private getCurrentOracleContract;
    private getOracle;
}
