import { Contracts } from './Contracts';
import { address, BaseValue, CallOptions, SendOptions } from '../lib/types';
export declare class Proxy {
    private contracts;
    private proxy;
    private perpetualV1;
    constructor(contracts: Contracts);
    getAdmin(options?: CallOptions): Promise<address>;
    getImplementation(options?: CallOptions): Promise<address>;
    initialize(token: address, oracle: address, funder: address, minCollateral: BaseValue, options?: SendOptions): Promise<any>;
    changeAdmin(newAdmin: address, options?: SendOptions): Promise<any>;
    upgradeTo(newImplementation: address, options?: SendOptions): Promise<any>;
    upgradeToAndCall(newImplementation: address, data: string, options?: SendOptions): Promise<any>;
}
