import BigNumber from 'bignumber.js';
import { Contracts } from './Contracts';
import { BigNumberable, CallOptions, Price, SendOptions, TradeResult, TxResult, address } from '../lib/types';
export declare function makeDeleverageTradeData(amount: BigNumberable, isBuy: boolean, allOrNothing: boolean): string;
export declare class Deleveraging {
    private contracts;
    private deleveraging;
    constructor(contracts: Contracts);
    get address(): string;
    trade(maker: address, taker: address, price: Price, amount: BigNumber, isBuy: boolean, allOrNothing?: boolean, traderFlags?: BigNumber, options?: CallOptions): Promise<TradeResult>;
    mark(account: address, options?: SendOptions): Promise<TxResult>;
    unmark(account: address, options?: SendOptions): Promise<TxResult>;
    getDeleveragingTimelockSeconds(options?: CallOptions): Promise<number>;
}
