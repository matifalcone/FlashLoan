import BigNumber from 'bignumber.js';
import { Contracts } from '../modules/Contracts';
import { CallOptions, SendOptions, MakerOracleMessage } from '../lib/types';
/**
 * Used to read and update the Maker Oracle V2 Medianizer contract.
 */
export declare class Relayer {
    private contracts;
    constructor(contracts: Contracts);
    get address(): string;
    /**
     * Get the timestamp of the latest update.
     */
    getAge(options?: CallOptions): Promise<BigNumber>;
    /**
     * Get the number of required signatures to update the oracle.
     */
    getBar(options?: CallOptions): Promise<number>;
    /**
     * Get the value of the latest update.
     */
    getValue(options?: CallOptions): Promise<BigNumber>;
    /**
     * Update the oracle.
     */
    poke(messages: MakerOracleMessage[], options?: SendOptions): Promise<void>;
}
