import { Contracts } from './Contracts';
import { SendOptions, TradeArg, TxResult } from '../lib/types';
import { TradeOperation } from './TradeOperation';
import { Orders } from './Orders';
export declare class Trade {
    private contracts;
    private orders;
    constructor(contracts: Contracts, orders: Orders);
    initiate(): TradeOperation;
    trade(accounts: string[], tradeArgs: TradeArg[], options?: SendOptions): Promise<TxResult>;
}
