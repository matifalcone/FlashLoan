import { Contracts } from './Contracts';
import { address, SendOptions, TxResult, BigNumberable } from '../lib/types';
export declare class Margin {
    private contracts;
    private perpetual;
    constructor(contracts: Contracts);
    deposit(account: address, amount: BigNumberable, options?: SendOptions): Promise<TxResult>;
    withdraw(account: address, destination: address, amount: BigNumberable, options?: SendOptions): Promise<TxResult>;
}
